<%= simple_form_for(@pothole) do |f| %>
 <%= f.input :address %>
  <%= f.input :size %>
  <%= f.input :description %>
  <%= f.input :status, collection: ['Reported', 'In progress', 'Done'] %>
  <%= f.submit %>
<% end %>
